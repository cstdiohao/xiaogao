server:
  port: 8083

spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/xiagao?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapping/*Mapping.xml
  type-aliases-package: com.nihao.entity

#showSql
logging:
  level:
    com:
      example:
        mapper : debug